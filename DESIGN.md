# Kindle Cards design

保留现有暖灰底色、暖白表面和陶土色动作按钮。界面使用系统中文无衬线字体，正文靠字号、行高和间距建立层级。

## 阅读列表

每条摘录为独立单列网格。标题行可收缩，正文自然换行，制作动作占独立底部行。禁止正文与按钮共用覆盖区域；列表不出现横向滚动。

## 卡片预览

预览依据工作区实际可用宽度缩放；像素尺寸与预览缩放独立。手机全屏固定 1080 × 1920，按真实内容高度调整字号；不能容纳时阻止静默截断导出。

## 小票主题

暖白纸面、细分隔线、克制的纸边纹理。顶部 Kindle Cards，正文保留段落，底部显示真实日期、书籍来源与阅读库统计，以装饰条码结束。界面控件沿用既有样式，主题只改变输出内容。
